subprojects {
    repositories {
        jcenter()
    }
    group = 'no.finntech.firetruck'
    version = '1.0-SNAPSHOT'
}

task wrapper(type: Wrapper) {
  gradleVersion = "2.14"
}

group = "no.finntech.firetruck"
